For running the web app with docker env: docker run -it -p 3000:3000 --rm -v $(pwd):/app reactenv
**This does not work anymore

Run the frontend from inside the frontend folder with: npm run electron:serve